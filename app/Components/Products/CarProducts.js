import { StyleSheet, Text, View, Image, Dimensions } from 'react-native';

export const MostrarProducto = ({image, points, cant})=>{
    return(<View style={styles.container}>
        <Text>Productos</Text>
    </View>
    );
}

const styles = StyleSheet.create({
    container:{
    },
})